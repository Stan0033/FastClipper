<Window x:Class="FastClipper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FastClipper"
        mc:Ignorable="d"
        WindowStyle="SingleBorderWindow"
    ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen" 
        Background="LightGray"
        Title="Fast Snipper" Height="90" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition  />
          
        </Grid.RowDefinitions>
       
            <TextBlock Text="hotkey 'PrtScr'. Save at:" VerticalAlignment="Center" Margin="5"/>

        <CheckBox Name="ChckMessage" IsChecked="True" Grid.Row="1" Margin="5,0,0,0" >Dislay message</CheckBox>
        <TextBox Name="SavePathTextBox" Margin="5" Grid.Column="1" IsReadOnly="True"/>
        <Button Grid.Column="2" Margin="5" Click="SelectFolder"> Change...</Button>
        <Button Grid.Column="3" Margin="5" Click="open">Open</Button>
        
    </Grid>
</Window>
